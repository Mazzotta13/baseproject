package com.alessio.baseproject.database.sql.dao.model;

// Unfortunately I had to use use the same names as classes (which is the default in Jackson), otherwise it wouldn't work when serializing
public class BatchAdditionalInfoSubType {
    public static final String BATCH_ADDITIONAL_INFO_CHANGE_EMAIL = "BatchAdditionalInfoChangeEmail";
}
